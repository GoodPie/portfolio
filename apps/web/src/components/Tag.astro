---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={["px-3 py-1 text-xs rounded-full bg-secondary text-secondary-foreground", className]}>
  <slot />
</span>
